name: Check if my workflow is Okay 💀

on: [push]

jobs:
  run-indexjs:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Run index.js
      run: |
        node index.js
